{
  "name": "@metric-org/datasource-fake",
  "private": true,
  "version": "1.0.0",
  "description": "Datasource fake para testes e validação de envio de dados (1000 tarefas, 1000 apontamentos).",
  "author": "Metric",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup --format esm --dts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@metric-org/sdk": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
