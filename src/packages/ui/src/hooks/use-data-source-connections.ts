import { useDataSourceConnections as useDataSourceConnectionsHook } from '@/contexts/DataSourceConnectionsContext'

// Alias hook (helps keeping naming consistent with other `use-*` hooks).
export const useDataSourceConnections = useDataSourceConnectionsHook
