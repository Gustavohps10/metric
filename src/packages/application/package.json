{
  "name": "@metric-org/application",
  "version": "1.0.0",
  "private": true,
  "description": "Contém os serviços e casos de uso (use cases) da aplicação Metric.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup --format esm,cjs --dts",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@metric-org/cross-cutting": "workspace:*",
    "@metric-org/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
